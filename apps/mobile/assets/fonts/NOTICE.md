# Fonts used by the Galaxia mobile app

Same static Fraunces and Inter instances as `apps/web/app/s/[token]/opengraph-image/fonts/`.
SIL Open Font License 1.1 (`OFL.txt`). See that folder's `NOTICE.md` for how they were subset.
