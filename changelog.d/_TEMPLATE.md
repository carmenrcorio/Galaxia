## <Short title> (branch `<your-branch>`) — <YYYY-MM-DD>

**Trigger**: why this change exists (one or two sentences).

`[TYPE]` **What changed.** The reason it changed, and anything a future reader
needs so it is not re-litigated or broken. Types: `DECISION`, `FIXED`, `ADDED`,
`CHANGED`, `REVERTED`, `BROKEN`, `OPEN`.
