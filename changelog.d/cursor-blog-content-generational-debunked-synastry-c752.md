## Three new blog posts + reciprocal synastry link (branch `cursor/blog-content-generational-debunked-synastry-c752`) — 2026-09-09

**Trigger**: `/blog` had one post (`synastry-chart-meaning`) and the `debunked` category (`/blog/debunked`) had zero — both empty content categories the sitemap already advertised. Three posts fill real competitor-gap keywords and reinforce the "people already in your life" moat (Generations, memorial profiles, Vela included-not-metered).

`[ADDED]` New migration `supabase/migrations/20260909180000_blog_posts_generational_debunked_synastry_aspects.sql`, following the same `INSERT ... on conflict (slug) do nothing` seam the original `synastry-chart-meaning` migration established — three published rows:
- `nobody-has-your-grandmother` ("Nobody's Astrology App Has Your Grandmother In It", category `guides`) — contrasts Galaxia's Generations/memorial-profile model against daily-horoscope, chart-calculator, and AI-reading competitor categories. Links to `/generations`, `/meet-vela`, `/synastry-chart-meaning`.
- `sun-sign-not-personality` ("Your Sun Sign Is Not Your Personality", category `debunked`) — the `debunked` category's first real post; `/blog/debunked` now renders it as a category listing entry rather than the empty-state note. Links to `/synastry-chart-meaning`, `/meet-vela`, `/why-galaxia`.
- `synastry-aspects-explained` ("7 Synastry Aspects That Predict How Relationships Feel", category `guides`) — mid-funnel deep dive on 7 named aspects (Venus conjunct Mars, Moon conjunct Moon, Moon square Saturn, Sun trine Sun, Mercury conjunct Mercury, Venus square Pluto, North Node conjunct personal planet). Links to `/synastry-chart-meaning`, `/generations`, `/meet-vela`.

`[CHANGED]` Same migration also updates the existing `synastry-chart-meaning` post body to add a reciprocal link to the new `synastry-aspects-explained` post (inserted after the "shared name for it" paragraph in the "Reading the catches" section) — the only change to that post's content; verified with a line-level diff against the original migrated body before applying.

All four posts (three new, one reciprocal edit) went through `computeReadTimeMinutes`'s exact `Math.max(1, Math.ceil(words/225))` rule by hand to keep `read_time_minutes` consistent with what the admin editor would compute (6, 5, 7 minutes respectively). Titles are all ≤60 characters and `dek` values ≤155 characters (both checked programmatically) since this template's `generateMetadata` uses `post.title`/`post.dek` directly for the `<title>` tag, OpenGraph/Twitter cards, and the on-page `<h1>` — there is no separate "SEO title" field.

Verified live: applied the migration directly to the one real Supabase project (`eigfvribtntbxyjutsma`, same one `20260909011620_blog_posts_schema_and_content.sql` targets), then ran `pnpm --filter @galaxia/web dev` locally against it (temporary `apps/web/.env.local`, gitignored, not committed) and confirmed in a real browser: all three new posts render at their top-level `/{slug}` URLs with correct title/meta description/Article JSON-LD (`mainEntityOfPage.@id` pointing at the right `https://galaxiamea.com/...` URL) and clickable internal links in both directions; `/blog/debunked` now lists `sun-sign-not-personality` instead of its empty-state note; `/blog` lists all four posts; `/sitemap.xml` includes all four post URLs with no duplicates. Screenshots taken during verification; no test data was left in Supabase (all four rows are real, intended content, not draft/test rows to clean up).

`[OPEN]` The task's target slugs used a `/blog/<slug>` URL shape (e.g. `/blog/nobody-has-your-grandmother`). That predates the current architecture: since `20260909011620_blog_posts_schema_and_content.sql`, every post is a single top-level dynamic route (`app/[slug]/page.tsx`), the same as the existing `/synastry-chart-meaning` — there is no nested `/blog/[slug]` route. New posts were published at their bare `/{slug}` URLs to match that pattern exactly, per the prompts' own instruction to "match that exact pattern." `/blog/debunked` is the category listing page (unaffected), not a post route.
