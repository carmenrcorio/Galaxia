## Blog hero images (branch `cursor/blog-hero-images-0697`) — 2026-09-15

**Trigger**: All ten published posts had `hero_image_url` null, so article pages and share cards fell back to the generic site OG image.

`[ADDED]` Programmatic 1200x630 SVG heroes (navy field, gold/cream serif title, small star cluster, `galaxiamea.com` wordmark) generated by `scripts/dev/generate-blog-hero-images.mjs` (dev tool, not a production import) and uploaded to the public `blog-images` Storage bucket. Migration `20260915013000_blog_hero_image_urls.sql` creates that SVG-only bucket and writes each public URL onto the matching published `posts` row.

`[CHANGED]` `app/[slug]/page.tsx` renders the hero `<img>` below the title and before the byline, with `width={1200}` `height={630}`, `max-width: 100%`, and `aspect-ratio: 1200 / 630` so the slot cannot shift. `buildPostMetadata` already preferred `hero_image_url` for `openGraph`/`twitter` images (falling back to `/og-image.png`); it now also states 1200x630 on the hero so crawlers get the og:image size.
