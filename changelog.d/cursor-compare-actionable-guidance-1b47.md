## Compare: actionable guidance + Person A name grammar fix (branch `cursor/compare-actionable-guidance-1b47`) — 2026-07-12

**Trigger**: Compare diagnosed but never prescribed — it named friction/flow aspects ("mercury square mars — friction that makes them grow", "venus trine mercury — so easy they don't notice it") with no way to act on them, and the "what X needs" prose described a need without saying how to meet it. Separately, the public Quick Compare rendered the ungrammatical "What You needs from you" whenever a logged-in user's own chart pre-filled Person A.

`[FIXED]` **Person A's real name now renders in Quick Compare (`/chart/compare`).** Root cause: the self pre-fill hardcoded `setNameA("You")` (a literal string), which was then dropped into third-person copy templates (`→ What {name} needs from you`, `{name}'s Cancer Moon means they need…`) producing "What **You** needs from you" — subject/verb disagreement and no real name. Fixed to resolve the self record's `display_name` via `useViewer().selfName` (the same pattern `/chart/page.tsx` already used); "You" survives only as a dead-code fallback since a self chart always has a required display name.

`[ADDED]` **Actionable, chart-grounded guidance per aspect (`lib/compare-guidance.ts`, rendered on `/app/compare` and `/chart/compare`).** For each surfaced aspect, `aspectActionLine()` adds a concrete "what to do" line derived from the two real bodies + the engine's `harmony` sign: a friction aspect gets a way to MINIMIZE the clash (e.g. Mercury–Mars square → "slow the pace and say 'I want to get this right with you' before you argue the point"), a flow aspect gets a way to NURTURE/USE the ease (e.g. Venus–Moon trine → "let the easy affection show… before it gets taken for granted"). Guidance is relationship-type aware via a per-type REGISTER (parent-child vs. siblings vs. friends vs. partners vs. ancestor read the same aspect differently) and `relationshipAspectFraming()` now guarantees at least one flow and one catch surface when both exist. No fabrication (§12): only real `from`/`to`/`type`/`harmony` are read — nothing is invented.

`[ADDED]` **"What X needs" strengthened to "need + how" (Phase 2).** `whatTheyNeed()` now appends a concrete delivery step tied to the same real placement (`MOON_HOW`/`VENUS_HOW`/`MERCURY_HOW`/`SATURN_HOW`, keyed to the person's engine-computed sign) — e.g. a Leo Moon's "needs to be genuinely seen and celebrated" now adds "say the specific thing you admire, out loud… not 'good job' but the actual detail".

**Minor safety (§9/§13):** the new guidance renders only inside the existing `!blockRomanticMinorRender` branch, so it inherits Compare's exact minor gate — for a pairing with a minor only non-romantic types are reachable, the non-romantic REGISTERs are the only ones that fire, and body tactics are authored in warmth/values terms (never desire/attraction). Covered by tests in `lib/compare-guidance.test.ts` (16 total).

**Voice:** every newly authored string is marked `// FOUNDER-REVIEW` for voice refinement. Untouched per §2: `next.config.mjs`, `.npmrc`, Vercel settings.
