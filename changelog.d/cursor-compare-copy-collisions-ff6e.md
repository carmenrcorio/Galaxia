## Compare aspect-summary collisions, duplicated person-card closer, stable dynamic rows (branch `cursor/compare-copy-collisions-ff6e`) — 2026-09-13

**Trigger**: Live platonic share (DANIEL & SARAH, token `vvgfUWf_oEKLJu68AEF9rw`) showed two copy defects and unstable table order: the bordered aspect-summary list under "Where it flows and catches" used one body for every catching aspect of a relationship type, both "What X needs from you" cards ended with the same Mercury-aspect sentence, and "Your dynamic" row order followed object-key iteration (live compute vs stored JSONB could disagree).

`[FIXED]` **Aspect-summary lines are keyed on the unordered planet pair.** `relationshipAspectFraming()` used to append `RELATION_ASPECT_FRAME[relType]` (one flows string and one catches string per relationship type) after every aspect citation. Mercury square Mars (0.9°) and Uranus square Jupiter (1.0°) therefore rendered the same body: "is where you talk past each other; slow down and confirm you mean the same thing." This is the same class of bug as the earlier ASPECT_ACTION leadBody collapse (lookup ignored the actual pair), in a different, previously unfixed table. New `ASPECT_SUMMARY_FRAME` (FOUNDER-REVIEW: authored) is keyed by `PAIR_KEY` like `ASPECT_ACTION`; `ASPECT_ACTION` itself is untouched. `RELATION_ASPECT_FRAME` remains only as a last-resort fallback.

`[FIXED]` **The platonic "real signal to watch" line renders once per reading.** It was computed from the shared synastry inside `whatTheyNeed()`, so both person cards closed with the identical relationship-level sentence. Moved to `relationshipWatchLine()` and rendered once below the cards on `/app/compare`, `/chart/compare`, `/s`, and Quick Check.

`[FIXED]` **Your dynamic rows have an explicit order, Overall first.** `Object.entries(scores)` followed insertion order on a live `computeSynastry()` result but alphabetical JSONB/parse order on stored shares. `SYNASTRY_SCORE_ORDER` / `orderedScoreEntries()` now define Overall, Emotional ease, Communication, Warmth, Shared values, Stability for every viewer.

Covered by `packages/astro/src/__tests__/aspect-summary-collisions.test.ts`, `synastry-score-order.test.ts`, and updates in `apps/web/lib/compare-guidance.test.ts`. Copy and ordering only: no change to aspect computation, orbs, or synastry scores. Untouched per §2: `next.config.mjs`, `.npmrc`, Vercel settings.
