## Compare self-preselection and sign-in mobile copy (branch `cursor/compare-self-preselection-signin-copy-c205`) — 2026-08-19

**Trigger**: UX re-review found Compare's relationship-type selector still defaulting to `friends` even when a saved person was tagged `Partner`. Phase 0 diagnosis (this branch) confirmed the tag→lens default logic itself (`suggestCompareRelationType`, shipped PR #77/#123) already resolves `self` + `partner` to `partners` correctly, with the minor clamp (`isMinorForSafety`) still winning last — the actual cause was Person A/B **auto-selection**: Compare preselects the two most-recently-created people, which can be two non-self people, so the self+tag inference correctly declines to guess and falls back to `friends`. Separately, the sign-in page was the last surface still using present-tense "as mobile" copy after signup/account/download were already corrected to future-tense.

`[ADDED]` **`initialComparePairIds` in `@galaxia/astro`.** A single shared helper (`packages/astro/src/compare-guidance.ts`) that prefers the user's `self` record as Person A and the most-recently-created other person as Person B on Compare's first load, falling back to today's "two most-recent" behavior when there is no `self` record. Both `apps/web/app/app/compare/page.tsx` and `apps/mobile/app/(app)/compare.tsx` now call this one function instead of duplicating the preselection logic, so the two surfaces cannot drift on this again. Web's existing `?a=<personId>` deep-link override (from a profile page) still takes priority over the self preference. This changes ONLY which two people start selected — both slots remain fully changeable afterward (including replacing self), and `defaultCompareRelationType`, `suggestCompareRelationType`, and the minor-clamp effect ordering are byte-for-byte unchanged on both apps.

`[FIXED]` **Sign-in mobile copy is now future-tense.** `apps/web/app/login/page.tsx` read "Use the same email + password account as mobile" (present tense, implying a live mobile app). Now reads "Use your Galaxia account on web today. When mobile links are live, this same account carries over." — matching the family already used on signup (`apps/web/app/signup/page.tsx`), account (`apps/web/components/get-app.tsx`), and download (`apps/web/app/download/page.tsx`). Tagged FOUNDER-REVIEW, no em dashes.
