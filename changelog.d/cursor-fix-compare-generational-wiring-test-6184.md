## Fix stale mobile Compare generational-wiring assertion (branch `cursor/fix-compare-generational-wiring-test-6184`) — 2026-09-13

**Trigger**: `apps/web/lib/compare-generational-section-wiring.test.ts` still asserted that mobile Compare (`apps/mobile/app/(app)/compare.tsx`) does **not** contain a native `GenerationalSection` ("deferred port"). PR #210 shipped that native port — `GenerationalSection`, `genFrame`, `genPlacement`, and `genHeadline` are now all wired in — so the negative assertion had been failing on `main` ever since.

`[FIXED]` **Flipped the stale negative assertion to a positive wiring guard.** The test now asserts mobile Compare imports and renders `GenerationalSection` from `apps/mobile/src/components/generational-section.tsx`, that component imports `buildGenerationalCallout` from `apps/mobile/src/lib/generational-callout.ts`, and that lookup module calls the curated `genFrame` / `genPlacement` / `genHeadline` lookups — same shape as the existing positive checks for the web surfaces in this file, so a future regression (e.g. someone reverting the native port, or wiring the component to the wrong data source) is still caught. `apps/mobile/src/lib/generational-callout.test.ts` (added in #210) already covers this wiring from the mobile side with different wording, so no test collision.
