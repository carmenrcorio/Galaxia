## Compare: curated generational call-out on Quick Compare + share (branch `cursor/generational-compare-section-2544`) — 2026-07-22

**Trigger**: Phase 0 persisted `generational.shared` + `generational.diverged` on compare snapshots, but both Quick Compare surfaces still painted only the vague `theme` line and a Shared sky notation strip — diverged was dropped entirely, and there was no curated cohort-level copy.

`[ADDED]` **Curated generational interpretation library (`packages/astro/src/generational-interpretations.ts`).** Static keyed lookup by `(planet, sign)` and by planet: essence + shared forward line per placement, domain + diverged watch-for per outer planet, and three section headlines selected by the engine's existing shared/diverged mix (`diverged.length === 0` → `allShared`; `shared.length >= 2` → `mostlyShared`; else `mostlyDiverged`). Fail-safe: `genPlacement` returns `null` on a miss so render never invents a specific meaning. FOUNDER-REVIEW on every authored string. Same discipline as `interpretations.ts`.

`[ADDED]` **Shared `GenerationalSection` consumed by `/chart/compare` and `CompareSnapshot` (`/s/<token>`).** One markup path for both surfaces. Renders the mix-selected headline, every shared placement (essence as meaning, shared as forward guidance, planet-in-sign notation demoted to proof), and every diverged placement (domain + watch-for + both-sides proof with essences). Missing library entries log the miss and fall back to an honest generic cohort line. Lookup only; no re-derivation of placements. `/app/compare` and `quick-check-modal` left untouched.
