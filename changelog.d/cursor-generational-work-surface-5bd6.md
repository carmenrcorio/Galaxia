## Generational work view (branch `cursor/generational-work-surface-5bd6`) — 2026-09-14

**Trigger**: The generational layer described a cohort's shadow, figures, and era events, but it led in astrology vocabulary and had no work-oriented surface for a recorded professional relationship.

`[DECISION]` **Phase 0 diagnosis lives in `packages/astro/src/generational-layer.ts`.** The layer is 100% curated static copy keyed to a confident Pluto sign (Uranus/Neptune live in `generational-interpretations.ts`). `corruptionSignature`, `plutoBridge`, and `FAMILY_BRIDGE` lead in astrology vocabulary. `eraEvents` and generation names were already sociological. Unauthored signs and Capricorn's empty figures stay skip-not-guess (§12).

`[ADDED]` **`eraReading`** on every authored Pluto sign: four plain-language fields (authority, institutions, change, trust) that lead. The placement stays visible as `plutoSourceLine(sign)` (`Source: Pluto in X`). Every claim traces to that sign's existing `corruptionSignature` / `eraEvents`. FOUNDER-REVIEW on every string.

`[ADDED]` **`workView`** on every authored Pluto sign: respect, decisions, friction under pressure. Static curated copy only. No runtime generation, no performance claims, no hiring guidance, no competence prediction (boundary comment in source, gated in tests). Shown only when the recorded relationship is professional (`isProfessionalRelation` on compare, `isProfessionalPersonRelation` on a person record).

`[CHANGED]` Compare generational sections (web `/app/compare`, share snapshots, mobile Compare) lead with the era reading, and with the work view plus a link to `/for-work` when the frame is professional. Person profiles (web + mobile) do the same when `person.relation` is a work tag.

`[CHANGED]` `/for-work` centrepiece now leads with the Virgo work view and era reading from the package (same sign as the Watergate / AIDS examples already quoted), then the era events. How it works names the source (`Source: Pluto in Virgo`) after the outcome.
