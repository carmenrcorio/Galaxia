## Groups: cohort headline keyed to Shared Sky (branch `cursor/groups-shared-sky-headline-bc37`) — 2026-07-23

**Trigger**: Group summary headlines claimed multiple active generational signatures while Shared Sky below often said there were no full-group shared outer-planet signatures. The label bucketed `faultLines.length`; Shared Sky reads `sharedSky.length`.

`[FIXED]` **`cohortOverlay().label` is keyed to `sharedSky.length`.** Same computed field the Shared Sky section renders, so headline and section cannot contradict. Authored FOUNDER-REVIEW headlines for `shared_3` / `shared_2` / `shared_1` / `shared_0`; fail-safe (empty/unclear) uses the `shared_0` framing. Deleted the fabricated multiple-signatures claim. Web and mobile both read `cohort.overlay.label`, so one engine change covers both.
