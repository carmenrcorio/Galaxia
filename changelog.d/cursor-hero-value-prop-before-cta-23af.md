## Homepage hero value proposition sits above the CTA (branch `cursor/hero-value-prop-before-cta-23af`) — 2026-09-14

**Trigger**: The anti-horoscope defense sat under the primary button, sandwiching the CTA between two lines of small type and asking for a click before the visitor had the premise. Destinations are unchanged.

`[CHANGED]` **Hero copy order.** The chart-for-each-person line and the Gemini defense now share the copy block above the button, so the visitor reads the premise first. FOUNDER-REVIEW. Headline is unchanged.

`[CHANGED]` **CTA microcopy** is only the friction reducer: "No card required · Works with just a birth date". The previous "No card. Works with just a birth date." line is gone. FOUNDER-REVIEW.

`[CHANGED]` **Secondary navigation** (Log in, See how it works) is its own lower-emphasis row under the primary action, so those links no longer sit in the same wrap as the gold CTA. Destinations unchanged (`/login`, `/#how`).
