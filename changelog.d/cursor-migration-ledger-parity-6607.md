## Reconcile migration ledger drift + CI parity check (branch `cursor/migration-ledger-parity-6607`) — 2026-09-11

**Trigger**: A readiness audit found production's applied-migration ledger on `eigfvribtntbxyjutsma` did not match `supabase/migrations/` in three places, the same class of failure as relational_transits/push_tokens and the em-dash post rewrite. Root cause is agents hand-running `apply_migration` (or `execute_sql`) without a shared ledger, not any one SQL file.

`[FIXED]` **`synastry_post_internal_links` applied verbatim** (`name=synastry_post_internal_links`, recorded as `20260911221148`). Confirmed live: `synastry-chart-meaning` body contains `/meet-vela`, `/generations`, and `/chart/compare`; `read_time_minutes` is 7.

`[FIXED]` **`people_star_color_and_memorial` ledger gap.** Columns already existed (nullable text, no comments) but had no `schema_migrations` row. No other committed file carries that DDL; PR #101 left "Apply migration" unchecked and later work created the columns out of band (comments were null until this apply). The file is idempotent (`ADD COLUMN IF NOT EXISTS`); applied as `name=people_star_color_and_memorial` (`20260911221147`) so the ledger matches and the missing `COMMENT ON COLUMN` statements landed.

`[ADDED]` **`20260911221500_profiles_timezone_capture_fix_search_path.sql`.** Production already has ledger row `20260820184406 profiles_timezone_capture_fix_search_path` with no matching file in git history (never added, renamed, or deleted). It was a hand-run `apply_migration` 43 seconds after `profiles_timezone_capture` during PR #137, pinning `validate_profile_timezone()` `search_path` to `''`. The original committed file already includes that pin; this new file is comment-only documentation so the repo has a permanent record. The production ledger row was not deleted or altered.

`[ADDED]` **Migration Ledger Parity CI** (`scripts/migration-ledger-parity.mjs` + `.github/workflows/migration-ledger-parity.yml`). Same shape as Edge Functions Parity: every push to `main`, daily cron (`41 13 * * *`), `workflow_dispatch`, reuses `SUPABASE_ACCESS_TOKEN`. Read-only `GET /v1/projects/{ref}/database/migrations`; **does not apply**. Identity is the snake_case name (filename suffix), because `apply_migration` stamps a new version. A file with no ledger name, or a ledger name with no file, fails with the exact two-way diff. Fixtures: matching pair (including version skew) exits 0; deliberate two-way gap exits 1. Documented in `ENGINEERING.md` §16 next to Edge Functions Parity.

`[FIXED]` **`synastry-chart-meaning` em dashes restored by the links apply, then rewritten.** Applying `synastry_post_internal_links` verbatim put 14 U+2014 characters back into the live body (that file predated `posts_rewrite_em_dash`). New migration `20260911233000_synastry_post_em_dash_keep_links.sql` keeps `/meet-vela`, `/generations`, and `/chart/compare`, and rewrites every dash-containing sentence the same way the original purge did (comma, colon, parentheses, two sentences; no hyphen substitution). FOUNDER-REVIEW. Applied as `name=synastry_post_em_dash_keep_links` (`20260911223101`). Live re-query: `like '%' || chr(8212) || '%'` is false; `position(chr(8212) in body)` is 0; all three links remain. `read_time_minutes` is 6: the previous 7 counted each dash as a word.
