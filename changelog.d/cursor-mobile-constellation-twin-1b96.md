## Phase 4 mobile constellation twin (branch `cursor/mobile-constellation-twin-1b96`) — 2026-09-17

**Trigger**: Twin spec Phase 4 after wheel (`#335`). Same living galaxy on Home and `/app`. No person-depth (Phase 5). Drag of `custom_position` is follow-on.

`[ADDED]` **Full-bleed Skia Home map.** `ConstellationMap` records atmosphere + motion Pictures from `galaxyGeometry` / `effectiveSeat` (rings, synastry beziers, dashed `RELATION_LINE_STYLE` honor/relation strokes, node forms, memorial glyphs, Inter 11 labels, meteors, EMA shed). Home drops the 340px glance card.

`[ADDED]` **Parity fetch + tap.** People select includes `star_color`; relationships select is un-narrowed (`person_a, person_b, relation_type`); `honorEdgesFromDeclaredRows` paints declared edges only. Tap a node → `/profile/[personId]`. `custom_position` plays back and freezes drift; reduced-motion paints one static sky after a 900ms fade.

`[TESTED]` Paint math (seats, freeze, bezier, hit-test, EMA), constellation-twin wiring, home empty/error/This Week order, web galaxy-interaction path now reads mobile `effectiveSeat` from the paint module. Metro boot + typecheck. Device FPS unverified (Cloud Agent cannot prove native UI at 375-class).

`[OPEN]` Device unverified. FPS at 375-class is device-only. Drag of custom seats stays follow-on. Phase 5 is person depth.
