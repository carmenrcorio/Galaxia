## Owner and thread indexes, concurrent form (branch `cursor/people-notes-messages-indexes-47c4`) — 2026-09-14

**Trigger**: Phase 0 against production `eigfvribtntbxyjutsma` confirmed `people.owner_id`, `notes.owner_id`, and `messages.thread_id` exist, and that `20260913030000_add_owner_and_thread_indexes.sql` already created `people_owner_id_idx`, `notes_owner_id_idx`, and `messages_thread_id_idx`. Row counts are people 47 / notes 8 / messages 92, so a concurrent build is not needed for lock duration. Two query patterns still lacked a covering index.

`[ADDED]` **Concurrent, idempotent restatement plus two composites** (`20260914023000_people_notes_messages_indexes.sql`): `people(owner_id)`, `notes(owner_id)`, `messages(thread_id)` via `CREATE INDEX CONCURRENTLY IF NOT EXISTS` (no-op where 20260913030000 already landed), `notes(about_person, created_at DESC)` for the mobile person-profile notes list and the unindexed `notes_about_person_fkey`, and `messages(thread_id, created_at DESC)` for constellation thread chips, Vela history, and Record thread preview. Concurrent index builds cannot run inside a transaction; this file must be applied with autocommit, not `supabase db push` / MCP `apply_migration`. No RLS changes.
