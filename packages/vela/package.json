{
  "name": "@galaxia/vela",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "echo 'vela lint pending'",
    "test": "echo 'vela tests pending'",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@galaxia/core": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
